@model IEnumerable<AdminSubjectListVM>
@{
    ViewData["Title"] = SharedLocalizer["List", SharedLocalizer["Subject"]];
    bool toggleState = ViewBag.ToggleState ?? false; // ViewBag.ToggleState'in değerini alıyoruz. Eğer null ise, varsayılan olarak false yapalım.
}

<section class="section dashboard">
    <div class="row">
        <div class="card recent-sales overflow-auto">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <h5 class="card-title">@SharedLocalizer["List", ViewLocalizer["Subject"]]</h5>
                    <a class="btn btn-primary my-4 col-lg-2" style="width:185px" data-bs-toggle="modal" data-bs-target="#createSubjectModal">@SharedLocalizer["Create", SharedLocalizer["Subject"]]</a>
                </div>
                <table class="table table-row-bordered datatable">
                    <thead>
                        <tr class="text-center">
                            <th class="text-center fw-bold text-white" style="background-color:rgb(134,138,155)">@Html.DisplayNameFor(x => x.Name)</th>
                            <th class="text-center fw-bold text-white " style="background-color: rgb(134,138,155)">@ViewLocalizer["Subject_Operations"]</th>                            
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var subject in Model)
                        {
                            <tr>
                                <td class="categories text-center">
                                    <a>
                                        <span class="post_category">@subject.Name</span>
                                    </a>
                                </td>
                                <td class="employment text-center">
                                    <div class="dropdown">
                                        <button id="optionsButton1">@SharedLocalizer["Options"] <i class="fa-solid fa-chevron-down"></i></button>
                                        <div class="dropdown-content">
                                            <a asp-action="Details" asp-route-id="@subject.Id">@SharedLocalizer["Details"]</a>
                                            <a asp-action="Update" asp-route-id="@subject.Id">@SharedLocalizer["Update"]</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>

<partial name="~/Areas/Admin/Views/Subject/CreatePartialView.cshtml" model="new AdminSubjectCreateVM()"></partial>

<script src="~/dataTable/assets/js/main.js"></script>
<script src="~/dataTable/~/js/main.js"></script>
<script src="~/lib/student/js/simple-datatables.js"></script>

<script>
    var classroomCount = @Model.Count();
    window.onload = function () {
        paginationSetupDropdown(classroomCount);
    }
</script>

